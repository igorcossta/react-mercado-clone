import React, { } from 'react';

import { Container } from './styles';
import MainBanner from '../../components/MainBanner';

const Home: React.FC = () => {
  return (
    <Container>
      <MainBanner />
    </Container>
  );
};

export default Home;
